{
  "frontend/src/components/dashboard/ProfileSection.tsx": "User profile editing component with photo upload gallery, personal/education/family detail tabs, and inline edit mode. Fixes Uint8Array BlobPart type error by using `as unknown as Uint8Array<ArrayBuffer>` cast.",
  "frontend/src/components/admin/CandidatesManagement.tsx": "Admin candidates management table with search/filter, approve/reject/suspend actions, add candidate modal (using direct `saveProfile`), and view profile dialog. Uses `useUpdateProfileStatus` and `useGetAllPhotos` to display photo counts from the photo store.",
  "frontend/src/components/admin/PaymentMonitoring.tsx": "Admin payment monitoring dashboard using `Payment.serviceType` and `Payment.status === 'confirmed'` (new API), with revenue stats and full transaction table.",
  "frontend/src/components/admin/LockedChatsMonitor.tsx": "Admin locked chats monitor showing metadata-only view using `useUnlockChat` and `LockedChat` type fields (`participantIds`, `lockedAt`, `reason`). Resolves participant names via `useGetAllProfiles`.",
  "frontend/src/index.css": "Global stylesheet defining the OKLCH-based matrimonial color system (saffron/gold/ivory palette), Google Fonts imports, and reusable component classes. Fixes the build error by removing the unsupported `border-border/60` opacity modifier from the `@apply` directive in `.card-matrimonial`, replacing it with plain `border-border`.",
  "frontend/src/pages/RegisterPage.tsx": "Four-step registration form with phone/email OTP verification. Removes `photoCount` and `isVisible` from the `register.mutateAsync` call since they are `Omit`-ted from the mutation parameter type.",
  "frontend/src/components/dashboard/MatchesSection.tsx": "Match suggestions and mutual matches dashboard with compatibility scoring, like/reject actions, phone blur/reveal (₹250 payment), and photo display. Fixes all `flex-shrink-0` → `shrink-0` Tailwind v3 warnings.",
  "frontend/src/components/dashboard/ChatsSection.tsx": "Complete rewrite of the chats section using the new localStorage/useQueries API, with payment gate (₹200 UPI), real-time polling chat thread, client-side contact detection that auto-locks chat, and locked chat display. Fixes the `flex-shrink-0` → `shrink-0` warning and removes all stale old-API imports.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for the matrimonial platform — auth (login/logout/register), OTP verification, profile CRUD, photo management, match suggestions, mutual matches, like/reject, chat unlock payments, messaging, chat locking/unlocking, phone reveal, and payment history. Adds `useIsCurrentUserAdmin` alias and fixes the `Uint8Array` BlobPart type error with a safe cast.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup for all routes. Wraps `RegisterPage` (which requires props) in a `RegisterPageWrapper` component so it can be used as a route component without props.",
  "frontend/src/pages/DashboardPage.tsx": "User dashboard page using `useCurrentUserId`/`useCurrentProfile` hooks (new API), passes `profile` prop to all child section components, and uses `photoCount` instead of `photos.length`.",
  "frontend/src/pages/AdminPage.tsx": "Admin dashboard page using `useCurrentUserId`/`useIsCurrentUserAdmin` hooks (new API), with access control guards and tabs for candidates, payments, and locked chats.",
  "frontend/src/components/layout/Header.tsx": "Sticky header with logo, navigation links, and auth controls using `useCurrentProfile`/`useIsCurrentUserAdmin`/`useLogout` hooks (new API), replacing all old Internet Identity hooks.",
  "frontend/src/components/dashboard/PaymentsSection.tsx": "User payment history section accepting `userId` prop, using `useGetPayments(userId)` with new `Payment.serviceType`/`status` fields and `payment.reference` instead of `referenceId`."
}